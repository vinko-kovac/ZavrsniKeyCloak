@page
@model IndexModel

@using Microsoft.AspNetCore.Authentication

<h2>User information</h2>

<dl>

    <dt>Name</dt>
    <dd>@User.Claims.First(x=> x.Type == "given_name").Value @User.Claims.First(x=> x.Type == "family_name").Value</dd>
    
</dl>

